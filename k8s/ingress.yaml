apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: catalog-microservice
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: "/products"
            backend:
              service:
                name: catalog-microservice
                port:
                  number: 80
